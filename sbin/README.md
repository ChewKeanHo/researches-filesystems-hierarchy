# `sbin`

The directory housing the readily usable system administration executables
and programs. When a project is locally built, all the sysadmin executables
including Windows' `.exe` binary files are housed here.

Unlike `bin`, only sysadmin and root and access these programs.
